- custom Path which is a wrapper around `Arc<PathBuf>`
- use rope for `Opened.entries` to avoid O(n) on deletion
- creating file
  - this should block
- renaming file
  - this should block
- selecting multiple files
- deleting selection
- renaming selection
- creating multiple files
- bulk operations
- handle opening files using mime types
- hooks for events
- sorting
- sorting using custom functions
- custom lua invocations via the `lua` command
- option for opening default editor when usign `lua` command
- visible columns as a lua function with state as an argument
- themeing
- custom keybindings
- merge redraw requests into a single redraw request
- feature flags
- minimal vim-like editor
- use `cfg-if`
